<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="deployment-notifications"/>
    
    <!-- add our web resources -->
    <web-resource key="deployment-notifications-resources" name="deployment-notifications Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="deployment-notifications.css" location="/css/deployment-notifications.css"/>
        <resource type="download" name="deployment-notifications.js" location="/js/deployment-notifications.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>deployment-notifications</context>
    </web-resource>
    
    <!-- publish our component -->
    <component key="myPluginComponent" class="com.carolynvs.bamboo.plugin.deployment_notifications.MyPluginComponentImpl" public="true">
        <interface>com.carolynvs.bamboo.plugin.deployment_notifications.MyPluginComponent</interface>
    </component>
    
    <!-- import from the product container -->
    <component-import key="applicationProperties" interface="com.atlassian.sal.api.ApplicationProperties" />
    
</atlassian-plugin>